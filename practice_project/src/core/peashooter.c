#include "peashooter.h"

void peashooter_init(struct peashooter *ps, struct pos pos)
{
    // if (NULL != ps)
    // {
    //     ps->pos = pos;
    //     ps->damage = 1;
    //     ps->health = 100;
    //     ps->state = IDLE;
    // }
}